import React from "react";
import Header from "../../Components/Stateless/Header/Header";

import "../../SCSS/_home.scss";

const Home = (props) => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
};

export default Home;
